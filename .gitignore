# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/

# SAM / build
.aws-sam/
samconfig.local.toml

# Secrets — never commit App keys or tokens
*.pem
.env
.env.*
!.env.example

# Local scratch
*.tar.gz
/tmp/
.DS_Store

# local dev venv
.venv/

# built into the package at deploy time; config/repos.yml is the source
src/config/
src/schemas/
reviewer/vendor/

# Replay tarball cache (fetched, never authored)
tests/fixtures/real/.tarballs/
